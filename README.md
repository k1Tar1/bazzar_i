# bazzar_i
A web and mobile platform for online sellers to create their own online stores, managing it and get analytical data of their business

TODO:
- add resendverification auth in check email page
- add limiter for register and resend email verification
- change alert message to success only if success status is received
- add the missing helper functions in AuthContext.jsx: setUser(updatedUser); setUser(updatedUser); function hasRole(role) {
    return user?.groups?.includes(role);
}


